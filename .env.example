# Notion data source ID for the "Daily Job Postings" database (POST /v1/data_sources/{id}/query).
NOTION_DATA_SOURCE_ID=
# SQLite cache path; the dedupe source of truth.
JOBS_DB_PATH=data/jobs.sqlite3
